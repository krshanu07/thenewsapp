class NewsModel {
  String title;
  String author;
  String desc;
  String url;
  String imageUrl;
  String source;
  int timestamp;

  NewsModel({this.title, this.author, this.desc, this.url, this.imageUrl, this.source, this.timestamp});
}